%li.li-clickable.product{data: {href: category_folder_product_path(category, folder, product)}}
  %h3.two-lanes-caption= product.name
  .product-image
    = image_tag category_picture(category)
  %span.price
    = t('words.price', price: product.price)
